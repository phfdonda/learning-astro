---
const { src, alt, width, height } = Astro.props;
---

<picture>
  <img src={src} alt={alt} />
</picture>

<style define:vars={{ width, height }}>
  img {
    width: 100%;
    max-width: 100%;
    object-fit: cover;
    max-inline-size: 100%;
    block-size: auto;
  }

  picture {
    width: var(--width);
    height: var(--height);
  }
</style>
